cat << 'EOF' > pyproject.toml
[project]
name = "fraud-detection"
version = "0.1.0"
description = "End-to-End Anti-Fraud ML Pipeline"
requires-python = ">=3.10"

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "W", "F", "I"]
EOF